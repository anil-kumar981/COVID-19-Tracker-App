import 'package:flutter/material.dart';

class CounteriesListScreen extends StatelessWidget {
  const CounteriesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
